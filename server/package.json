{
  "name": "server",
  "version": "1.0.0",
  "description": "Socket and sandbox execution service for Code Golf Arena",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": ">=20.9 <25"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test \"{test,tests}/**/*.test.js\"",
    "check": "node --check index.js && node --check executor.js && node --check judge.js && npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dockerode": "^5.0.0",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
